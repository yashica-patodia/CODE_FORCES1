#include<bits/stdc++.h>
using namespace std;
typedef long long int lli;
//int check(lli mm,lli d,vector<lli>vec,lli n);
int main()
{
	int t;
	cin>>t;
	int temp=t;
	while(t--)
	{
		lli arr[1000000000][1000000000];
		string str;
		cin>>str;
		for(i=0;i<str.length();i++)
		{
			if(isdigit(str[i]))
			{
				int dig=stoi(str[i]-'0');

			}
			ans+=str[i];
		}
	}
}